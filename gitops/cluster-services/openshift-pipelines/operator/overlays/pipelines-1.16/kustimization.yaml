namespace: openshift-operators

resources:
  - ../../base

patches:
  - patch: |-
      - op: replace
        path: /spec/channel
        value: pipelines-1.16
    target:
      group: operators.coreos.com
      version: v1alpha1
      kind: Subscription
      name: openshift-pipelines-operator
      namespace: openshift-operators