apiVersion: monitoring.rhobs/v1
kind: ServiceMonitor
metadata:
  name: kuadrant-authorino-opeartor-monitor
  labels:
    metrics: workload-monitor 
spec:
  endpoints:
  - path: /metrics
    port: metrics
    scheme: http
  selector:
    matchLabels:
      control-plane: authorino-operator