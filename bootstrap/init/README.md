# Init

Seed the cluster with:
* Doppler credentials for External Secrets.
* OpenShift Gitops Operator
* RHACM Operator
* Bootstrap GitOps App (basic config + RHACM instance/policies/policysets/etc...)